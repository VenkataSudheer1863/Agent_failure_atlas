# Llama-3.3-70B — Groq Configuration (Large tier)
# Provider: Groq (verified available 2026-06-15)

model_name: "Llama-3.3-70B"
tier: "large"

provider:
  name: groq
  api_key_env: GROQ_API_KEY_4
  base_url: "https://api.groq.com/openai/v1"
  model_id: "llama-3.3-70b-versatile"

generation:
  temperature: 0.0
  max_tokens: 2048
  seed: 42
  timeout_seconds: 60

notes: >
  Llama 3.3 70B serves as the large-tier baseline in the AFA benchmark.
  Served via Groq LPU for ultra-low-latency inference.
