# Llama-4-Scout-17B — Groq Configuration (Medium tier)
# Provider: Groq (verified available 2026-06-15)

model_name: "Llama-4-Scout-17B"
tier: "medium"

provider:
  name: groq
  api_key_env: GROQ_API_KEY_2
  base_url: "https://api.groq.com/openai/v1"
  model_id: "meta-llama/llama-4-scout-17b-16e-instruct"

generation:
  temperature: 0.0
  max_tokens: 2048
  seed: 42
  timeout_seconds: 90

notes: >
  Llama 4 Scout 17B serves as the medium-tier model in the AFA benchmark.
  Uses a Mixture-of-Experts architecture (17B total, 16 experts).
  Served via Groq LPU for low-latency inference.
