# Qwen3-32B — Groq Configuration (Reasoning tier)
# Provider: Groq (verified available 2026-06-15)

model_name: "Qwen3-32B"
tier: "reasoning"

provider:
  name: groq
  api_key_env: GROQ_API_KEY_3
  base_url: "https://api.groq.com/openai/v1"
  model_id: "qwen/qwen3-32b"

generation:
  temperature: 0.0
  max_tokens: 4096
  seed: 42
  timeout_seconds: 180

notes: >
  Qwen3-32B serves as the reasoning-tier model in the AFA benchmark.
  Provides chain-of-thought reasoning via <think>...</think> traces,
  which are automatically stripped before evaluation to extract the final answer.
